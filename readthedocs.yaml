# .readthedocs.yaml

version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.9"

python:
  install:
    - requirements: docs/requirements.txt

# Build documentation using Sphinx
sphinx:
  configuration: docs/source/conf.py

